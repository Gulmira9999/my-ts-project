import React, { FC } from "react";
import "./App.css";
interface AppProps {}

const App: FC<AppProps> = () => {
  return (
    <div className="App">
      <header className="App-header">Learn React</header>
    </div>
  );
};

export default App;
